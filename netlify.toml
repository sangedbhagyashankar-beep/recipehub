[build]
  command = "pnpm install && cp -r frontend/public src/frontend/public && pnpm --filter '@caffeine/template-frontend' build:skip-bindings"
  publish = "src/frontend/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
